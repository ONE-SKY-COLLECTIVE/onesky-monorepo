import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function TabTwoScreen() {
  return (
    <SafeAreaView>
      <Text className="text-red-500 text-7xl "> I am styled using nativewind</Text>
    </SafeAreaView>
  );
}
